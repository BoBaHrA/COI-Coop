# COI-Coop

Experimental co-op mod for Captain of Industry.

Development is currently on `dev/network-foundation`.

The first milestone is a local two-instance handshake plus bidirectional ping. It is intentionally restricted to loopback until the transport is proven inside the game.

See `docs/TESTING.md` for the current test procedure.
